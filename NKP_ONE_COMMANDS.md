# NKP_ONE บอร์ด - สรุปคำสั่งทั้งหมด

## 📋 สารบัญ
1. [เริ่มต้น](#เริ่มต้น)
2. [มอเตอร์](#มอเตอร์)
3. [เซอร์โว](#เซอร์โว)
4. [OLED Display](#oled-display)
5. [สวิตช์](#สวิตช์)
6. [บัซเซอร์](#บัซเซอร์)
7. [ขา IO](#ขา-io)
8. [เซนเซอร์ตรวจจับสี](#เซนเซอร์ตรวจจับสี-tcs)

---

## เริ่มต้น

### เริ่มต้นบอร์ด NKP_ONE
```cpp
#include <NKP_ONE.h>

void setup() {
  Serial.begin(115200);  // เริ่มต้น Serial
  NKP_ONE();             // เริ่มต้นบอร์ด (OLED, Motor PWM, etc)
}
```

### รอกดปุ่ม SW1 ก่อนเริ่มโปรแกรม
```cpp
IO15();  // แสดง "press sw1 to start" แล้วรอกดปุ่ม
```

---

## มอเตอร์

### ควบคุมมอเตอร์แต่ละตัว
```cpp
motor(pin, speed)   // ควบคุมมอเตอร์ (pin: 1 หรือ 2, speed: -100 ถึง 100)
```

**ตัวอย่าง:**
```cpp
motor(1, 50);    // มอเตอร์ 1 ไปข้างหน้า ที่ความเร็ว 50%
motor(2, -50);   // มอเตอร์ 2 ถอยหลังที่ความเร็ว 50%
motor(1, 0);     // หยุดมอเตอร์ 1
```

### ฟังก์ชันการเคลื่อนที่

| คำสั่ง | คำอธิบาย | ตัวอย่าง |
|--------|---------|---------|
| `fd(speed)` | ไปข้างหน้า (Forward) | `fd(50)` - ไปข้างหน้า 50% |
| `bk(speed)` | ถอยหลัง (Backward) | `bk(50)` - ถอยหลัง 50% |
| `sl(speed)` | เลี้ยวซ้าย (Spin Left) | `sl(50)` - หมุนซ้าย |
| `sr(speed)` | เลี้ยวขวา (Spin Right) | `sr(50)` - หมุนขวา |
| `ao()` | หยุด (All Off) | `ao()` - หยุดทันที |

### ฟังก์ชันขั้นสูง

```cpp
MT(speed1, speed2, time)  // ควบคุมมอเตอร์สองตัวพร้อมกัน + delay
// ตัวอย่าง: MT(50, -50, 1000) - มอเตอร์1 ไปข้างหน้า มอเตอร์2 ถอยหลัง เป็นเวลา 1 วินาที

motor_S(pin, speed)       // ฝังไว้ (หมายเหตุ: ใช้ motor() ดีกว่า)

aot(time)                 // หยุดและ delay
// ตัวอย่าง: aot(500) - หยุดเป็นเวลา 500 มิลลิวินาที
```

---

## เซอร์โว

### ควบคุมเซอร์โว
```cpp
servo(servo_number, angle)

// servo_number: 1, 2, 3, 4 หรือ ALL (ควบคุมทั้งหมด)
// angle: 0-180 องศา หรือ -1 เพื่อปิด (detach)
```

**ตัวอย่าง:**
```cpp
servo(1, 0);      // เซอร์โว 1 ไปที่ 0 องศา
servo(1, 90);     // เซอร์โว 1 ไปที่ 90 องศา
servo(1, 180);    // เซอร์โว 1 ไปที่ 180 องศา
servo(ALL, 90);   // เซอร์โวทั้งหมดไปที่ 90 องศา
servo(1, -1);     // ปิดเซอร์โว 1
servo(ALL, -1);   // ปิดเซอร์โวทั้งหมด
```

**ขาเซอร์โว:**
- Servo 1: GPIO23
- Servo 2: GPIO19
- Servo 3: GPIO18
- Servo 4: GPIO5

---

## OLED Display

### แสดงข้อความ
```cpp
set_oled(x, y, text)      // แสดงข้อความ (text: char array)
set_oled(x, y, number)    // แสดงตัวเลข (number: int)
```

**ตัวอย่าง:**
```cpp
set_oled(0, 0, "Hello");       // แสดง "Hello" ที่ (0, 0)
set_oled(0, 10, 12345);        // แสดงเลข 12345 ที่ (0, 10)
set_oled(10, 20, "Speed: ");   // แสดง "Speed: " ที่ (10, 20)
```

### ควบคุม OLED

| คำสั่ง | คำอธิบาย |
|--------|---------|
| `set_oled_clear()` | ลบเนื้อหา OLED ทั้งหมด |
| `set_oled_show()` | อัปเดต/แสดง OLED (ใช้หลังจาก `set_oled_S`) |
| `set_oled_S(x, y, text)` | แสดงข้อความโดยไม่อัปเดตหน้าจอทันที |

**ตัวอย่าง:**
```cpp
set_oled_clear();           // เคลียร์หน้าจอ
set_oled(0, 0, "Start");    // แสดง "Start"
delay(1000);
set_oled_clear();           // เคลียร์อีกครั้ง
set_oled(0, 0, "Stop");     // แสดง "Stop"
```

---

## สวิตช์

### อ่านสถานะสวิตช์ SW1 (GPIO15)
```cpp
sw1()     // อ่านสถานะ SW1 (ส่งกลับ 1 หรือ 0)
SW1()     // เหมือน sw1() (ตัวอักษรใหญ่)
sw_ok()   // เหมือน sw1()
```

**ตัวอย่าง:**
```cpp
if (sw1() == 0) {
    // กดปุ่มแล้ว (LOW = pressed)
    beep();
}

// รอปุ่มกด
while(sw1() == 1) {
    delay(10);  // หากปุ่มยังไม่ได้ถูกกด
}
```

---

## บัซเซอร์

### ปล่อยเสียง
```cpp
beep()  // ปล่อยเสียง beep (500ms ON, 500ms OFF)
```

**ตัวอย่าง:**
```cpp
beep();          // ปล่อยเสียง
delay(500);
beep();          // ปล่อยเสียงอีกครั้ง
```

**ขา:** GPIO12

---

## ขา IO

### อ่านค่า Analog (ADC)

```cpp
analog(pin)  // อ่านค่า analog ของขา (pin: 0-9)
```

**ขา Analog:**
| Pin | GPIO |
|-----|------|
| 0 | 39 |
| 1 | 34 |
| 2 | 32 |
| 3 | 35 |
| 4 | 25 |
| 5 | 33 |
| 6 | 27 |
| 7 | 26 |
| 8 | 13 |
| 9 | 14 |

**ตัวอย่าง:**
```cpp
int value = analog(0);     // อ่านค่า analog pin 0 (GPIO39)
Serial.println(value);     // ส่งไปยัง Serial monitor
```

### ควบคุม Digital IO

```cpp
in(pin)              // อ่านค่า digital (pin: GPIO number)
in_pullup(pin)       // อ่านค่า digital พร้อม pull-up
out(pin, state)      // ตั้งค่า digital (state: HIGH หรือ LOW)
```

**ตัวอย่าง:**
```cpp
int button = in(14);              // อ่านปุ่มที่ GPIO14
int sensor = in_pullup(12);       // อ่าน sensor พร้อม pull-up
out(14, HIGH);                    // ตั้ง GPIO14 เป็น HIGH
out(14, LOW);                     // ตั้ง GPIO14 เป็น LOW
```

### เซนเซอร์ระยะทาง (Infrared Distance)

```cpp
getdist(pin)  // คำนวณระยะทาง (ซม.) จากค่า analog
```

**ตัวอย่าง:**
```cpp
float distance = getdist(0);   // อ่านระยะทางจาก pin 0
Serial.print("Distance: ");
Serial.print(distance);
Serial.println(" cm");
```

---

## เซนเซอร์ตรวจจับสี (TCS)

### ตั้งค่าเซนเซอร์

```cpp
setSensorPins(pins_array, num_sensors)   // ตั้งค่า pins ของเซนเซอร์
setSensorMin(min_array)                  // ตั้งค่า min calibration
setSensorMax(max_array)                  // ตั้งค่า max calibration
setSensitive(value)                      // ตั้งค่าความอ่อนไหว (default: 300)
```

### Calibration

```cpp
setCalibrate()  // Calibrate เซนเซอร์โดยอัตโนมัติ
```

### อ่านค่าเซนเซอร์

```cpp
readline()                 // อ่านตำแหน่งเส้น
ReadSensorMinValue(index)  // อ่านค่า min ของเซนเซอร์
ReadSensorMaxValue(index)  // อ่านค่า max ของเซนเซอร์
```

**ตัวอย่าง:**
```cpp
void setup() {
  Serial.begin(115200);
  NKP_ONE();
  
  // ตั้งค่า 8 เซนเซอร์
  int pins[] = {39, 34, 32, 35, 25, 33, 27, 26};
  setSensorPins(pins, 8);
  
  // Calibrate
  set_oled_clear();
  set_oled(0, 0, "Calibrating...");
  setCalibrate();
  delay(2000);
}

void loop() {
  int position = readline();  // อ่านตำแหน่งเส้น
  Serial.println(position);
  
  // ควบคุมมอเตอร์ตามตำแหน่ง
  if (position < 3000) {
    sl(50);    // เลี้ยวซ้าย
  } else if (position > 4000) {
    sr(50);    // เลี้ยวขวา
  } else {
    fd(50);    // ไปข้างหน้า
  }
  
  delay(50);
}
```

---

## ตัวอย่างโปรแกรมสมบูรณ์

```cpp
#include <NKP_ONE.h>

void setup() {
  Serial.begin(115200);
  NKP_ONE();
  
  // แสดงข้อความเริ่มต้น
  set_oled_clear();
  set_oled(0, 0, "NKP ONE");
  set_oled(0, 10, "Ready!");
  
  // รอกดปุ่ม
  IO15();
  
  // ปล่อยเสียง
  beep();
}

void loop() {
  // ไปข้างหน้า
  fd(50);
  delay(2000);
  
  // หมุนซ้าย
  sl(50);
  delay(1000);
  
  // หยุด
  ao();
  delay(1000);
  
  // เซอร์โว
  servo(1, 0);
  delay(500);
  servo(1, 90);
  delay(500);
  servo(1, 180);
  delay(500);
}
```

---

## คำเตือน

- **ค่าความเร็ว:** -100 ถึง 100 (ลบ = ถอยหลัง, บวก = ไปข้างหน้า)
- **มุมเซอร์โว:** 0-180 องศา
- **OLED Coordinates:** (x, y) - x: 0-127, y: 0-63 (ระยะ 8px ต่อแถว)
- **PWM Pins:** Motor ใช้ PWM (GPIO 2, 4, 16, 17)
- **I2C:** OLED ใช้ I2C ที่ GPIO 21 (SDA) และ 22 (SCL)

---

**ที่มา:** NKP_ONE Library v1.0.0
**อัปเดต:** January 2026
